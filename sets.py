# sets.py

# every index is unique, not sortable, not indexed

my_set = {'one','two', 'three', 4, 'one'}
print(my_set)

# create unique data sets without duplicates

# add and remove function

my_set.add('five')
print(my_set)

my_set.remove(4)
print(my_set)